create repository in GitHub and copy the URL

git clone https://github.com/ZhangMYihua/lesson-1.git cloned-repo    //cloning foreign repo
 git remote remove origin
git remote
git remote add origin git@github.com....      //repo link (to my local repo)
git push origin master

git remote add origin (copied url from gitHub)    //repo link (to my local repo)
git status
git add -A
git commit -m "created react app"
git push origin



